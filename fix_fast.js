const fs = require("fs"); let c = fs.readFileSync("fast.js", "utf8"); c = c.replace(/let content = fs\.readFileSync\\(.*\\);/, ""); c = c.replace("function extract(arrName) {", "function extract(arrName, content) {"); c = c.replace("let ext = extract(section);", "let content = fs.readFileSync(\"src/app/data/book-data.ts\", \"utf8\");\n        let ext = extract(section, content);"); c = c.replace("let newExt = extract(section);", "let newExt = extract(section, newContent);"); fs.writeFileSync("fast_fixed.js", c);
